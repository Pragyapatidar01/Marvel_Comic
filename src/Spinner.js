import React from 'react'
import spinner from './img/spinner-black.gif'

const Spinner = () => {
    return (
        <img src={spinner} style={{margin:'auto',display:'block' }} alt="Loading"></img>
    )
}

export default Spinner
